#include <studio.h>
/**
 * main -Entry point
 *
 * Return: Always return  0 (Sucess/Crrect)
 */

int main(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}

putchar('\n');

return (0);
}
