#include <stdio.h>
/**
 * main - Entry point
 * 
 * Return: Always 0 (sucess/correct)
 */
int main(void)
{
int digit;

for (digit = o; digit < 10; digit++)
{
ptrintf("%digit", digit);
}

printf("\n");

return (0);

}
